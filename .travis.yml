language: node_js
node_js:
  - "10.17.0"
dist: trusty
sudo: required
branches:
  only:
    - master
before_script:
  - npm install -g @angular/cli
script:
  - ng lint
  - ng test tfaster-extended-cdk-selection --watch false --browsers ChromeHeadless
  - ng build --prod --base-href https://github.com/tFaster/tfaster-extended-cdk-selection/
deploy:
  provider: pages
  skip_cleanup: true # To avoid cleanup of the artifact generated during script
  github_token: $GITHUB_TOKEN # Provided as an environment variable in travis
  local_dir: dist/tfaster-extended-cdk-selection # Replace this with your project name
  on:
    branch: master
